rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
  	match /products/{productFolder}/{productImage} {
  		allow write, read;
  	}
    match /test-products/{productFolder}/{productImage} {
  		allow read, write;
  	}
  }
}
